
──────────────────────────────────────────────────────────────────────────────
D30  Minigame de bola parada desligado desde a R18
     severidade: higiene   fase: F1   estado: decidir

  DONO (quem realmente executa):
     produto
  CAMADAS QUE INTERCEPTAM: 63, 64
     confirme com: node tools/fisica/pilha.js dist/index.html 14

  EVIDENCIA MEDIDA:
     15 linhas mortas + 2 pontos de chamada que testam input==null sem necessidade + camadas 63/64 (255 linhas) de status desconhecido

  CRITERIO DE ACEITE:
     - pilha.js diz se 63/64 rodam, ANTES de decidir

  RISCO: baixo se medido antes

──────────────────────────────────────────────────────────────────────────────
CODIGO ATUAL (localizado por ancora, nao por numero de linha)
──────────────────────────────────────────────────────────────────────────────

  src/scripts/40-match-engine-and-manager-ai.js:378   (o documento dizia linha 369 — o codigo andou)

      366 │ 
      367 │   /* Suspende somente o relógio/física enquanto o jogador prepara a cobrança.
      368 │      O callback resolve uma única vez e devolve o controle ao motor. */
      369 │   /* MOTOR VISUAL · minigames de falta e pênalti DESATIVADOS desde a R18: as
      370 │      cobranças resolvem no motor, com voo real e goleiro convergindo.
      371 │ 
      372 │      D03 · aqui havia 17 linhas montando a requisição interativa, todas depois
      373 │      de um `return false;` — inalcançáveis, e escritas de forma a parecerem
      374 │      vivas. Foram removidas. A reconexão futura de um minigame não passa por
      375 │      este método: passa por `_freeKick` e `_penalty`, que são os donos do lance.
      376 │      Os dois pontos de chamada continuam testando `input == null` por hábito;
      377 │      ver D30, que é a decisão de produto pendente. */
  >   378 │   _requestSetPiece() { return false; }
      379 │ 
      380 │   _switchSides() {
      381 │     for (const tm of this.teams) {
      382 │       tm.attackDir *= -1;
      383 │       const g = tm.goal; tm.goal = tm.oppGoal; tm.oppGoal = g;
      384 │       for (const p of tm.players) {
      385 │         p.hx = FL - p.hx;  p.hy = FW - p.hy;
      386 │         p.dhx = FL - p.dhx; p.dhy = FW - p.dhy;
      387 │         p.ahx = FL - p.ahx; p.ahy = FW - p.ahy;
      388 │         p.x = FL - p.x;    p.y = FW - p.y;
      389 │         p.vx = 0; p.vy = 0;
      390 │       }

──────────────────────────────────────────────────────────────────────────────
DOCUMENTO — reports/INVESTIGACAO-COMPLETA-2026-08.md:3137-3173
──────────────────────────────────────────────────────────────────────────────
  v5.2.2 elevou deliberadamente os *shares* de escanteio em ~25%:
  
  ```js
      /* CALIBRAÇÃO (auditoria v5.2.2): escanteios mediam 4.08/partida com a
         faixa de design em 5.0–11.5. Shares elevados ~25% de forma distribuída
         — desvios, bloqueios e bolas na trave passam a morrer mais na linha de
         fundo, como no futebol real (~10/jogo). */
      lowCrossSaveCorner: 0.55,
      failedCrossCorner: 0.76,
      /* ... mais seis ... */
  ```
  
  Note que o comentário diz "faixa de design em 5.0–11.5" enquanto
  `ENGINE_CALIBRATION.targets.corners` diz `[3, 9]`. **Há duas faixas de design
  conflitantes no mesmo arquivo.**
  
  **A mudança proposta.** Reconciliar: `targets.corners` passa a `[8, 13]`,
  alinhado ao futebol real e ao comentário da própria calibração. É correção de
  alvo, não de jogo.
  
  **Atenção — D08 vai empurrar isto para baixo.** Bola que sai pela lateral não
  sai pelo fundo. Fazer a reconciliação **depois** de D08, ou o alvo terá de ser
  mexido duas vezes.
  
  ---
  
  ## D30 🟡 O minigame de bola parada está desligado desde a R18
  
  **Endereço:** `src/scripts/40-match-engine-and-manager-ai.js:369`
  
  ```js
    _requestSetPiece(kind, data, execute) {
      /* MOTOR VISUAL · minigames de falta e pênalti DESATIVADOS [...]
         A arquitetura de requisição fica preservada para reconexão futura */
      return false;
  ```
  

──────────────────────────────────────────────────────────────────────────────
ANTES DE EDITAR
──────────────────────────────────────────────────────────────────────────────
  node tools/fisica/pilha.js dist/index.html 14   # confirme o DONO acima
  bash tools/aceitar.sh --antes
  # ... edite src/, NUNCA dist/ ...
  bash tools/aceitar.sh --depois

